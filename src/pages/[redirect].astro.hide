
export function getStaticPaths() {
  return [
    // Generates: /dogs/clifford
    {params: {redirect: '2022/08/31/migrating-from-heroku-postgres-to-local.html'}},
    // Generates: /dogs/rover
    {params: {dog: 'rover'}},
    // Generates: /dogs/spot
    {params: {dog: 'spot'}},
  ];
}

const { redirect } = Astro.props;